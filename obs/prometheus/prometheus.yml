global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "pagatu-microservicios"
    eureka_sd_configs:
      - server: "http://pagatu-eureka:8761/eureka"
    metrics_path: "/actuator/prometheus"
    relabel_configs:
      - source_labels: [__meta_eureka_app_name]
        regex: "PAGATU-CATALOGO-MS|ORDEN-MS"
        action: keep
      - source_labels: [__meta_eureka_app_name]
        target_label: application
